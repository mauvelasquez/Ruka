export const metadata = {
  title: 'Mis matches — Rukka',
  description: 'Revisa tus matches de intercambio de hogares en Rukka.',
  robots: {
    index: false,
    follow: false,
  },
}

export default function MatchesLayout({ children }) {
  return children
}
